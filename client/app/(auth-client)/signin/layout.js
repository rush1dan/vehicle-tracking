export const metadata = {
    title: 'SignIn',
    description: 'Tracky - SignIn',
}

export default function SignInLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
