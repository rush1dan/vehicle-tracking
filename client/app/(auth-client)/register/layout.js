export const metadata = {
    title: 'Register',
    description: 'Tracky - Register',
}

export default function RegisterLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
